Connect Admin Dashboard with Frontend and improve Settings section.

1. Full Sync between Admin and Frontend:
- All changes in admin panel must automatically update frontend using the same database and API.

Games / Products:
- When admin adds a game/product, it must appear on frontend automatically.
- Edit changes must update frontend instantly.
- If disabled or deleted, hide/remove from frontend.
- After adding a game, include an action button or toggle option:
  "Highlight in Trending"
- When enabled, the game should appear in the "Trending Now" section on the frontend.
- Admin should be able to enable or disable trending status anytime.

Orders:
- Orders created on frontend must appear in admin dashboard.
- Status updates (approved, rejected, completed) must sync on frontend user order page.

Users:
- Users registered on frontend must appear in admin users page.
- If admin disables a user, user login should be blocked.

Transactions:
- Frontend payments must appear in admin transactions.
- Status updates must sync both sides.

Support Tickets:
- Tickets submitted on frontend must appear in admin support page.
- Admin replies must show on frontend user dashboard.

2. API and Database:
- Use same database for admin and frontend.
- Create functional API routes:
  GET (fetch data)
  POST (create data)
  PUT/PATCH (update data)
  DELETE (remove data)

3. Auto update:
- Changes in admin should reflect on frontend quickly using API refetch or realtime update.

4. Settings Improvements:
Add "Plugins" page inside Settings section.

Plugins page requirements:
- show list of installed plugins/modules
- toggle enable/disable plugin
- install plugin option
- remove plugin option
- plugin status indicator (active/inactive)
- save configuration button
- plugin settings area if plugin has options

Example plugins:
- sms/otp service
- email service
- analytics
- webhook integration

All buttons must be functional and connected to backend logic.

5. API Integration page change:
- There is already a dedicated Payment page.
- Remove "Add Payment Gateway" option from API Integration page.
- Payment gateway configuration should exist only inside the Payment page.

6. Remove mock frontend games:
- Remove all mock/demo games shown on frontend below the "Trending Now" section.
- Frontend should only display games that exist in the database.
- If no games exist, show empty state message like "No games available".

7. Functional controls across dashboard:
Ensure useful buttons exist and work correctly:
- search bars
- filters
- date range filters
- approve/reject buttons
- enable/disable toggles
- edit/delete buttons
- save/update buttons

8. Remove mock/demo data everywhere:
- delete all fake items from database and UI
- show real data only
- if empty, show message like "No data available"

Maintain existing dark theme UI.
Keep responsive layout.
Use reusable components where possible.